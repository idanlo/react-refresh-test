import React from 'react';
import './test.scss';

const Test = () => {
    return (
        <div className="test">
            <h1>Nested Component (no need for refresh)</h1>
        </div>
    );
};

export default Test;
